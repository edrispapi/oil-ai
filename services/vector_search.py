# شِل پایه جستجوی برداری (FAISS یا Pinecone)
def search_documents(query, top_k=5):
    """
    بازیابی برترین اسناد مطابق شباهت معنایی با پرسش
    """
    # TODO: پیاده‌سازی encode و جستجو در وکتوربیس
    # مقدار بازگشتی: لیستی از اسناد یا متن‌های مهم یافت شده
    return ["نمونه سند ۱", "نمونه سند ۲"]
